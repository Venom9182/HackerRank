export default function RecentlyViewed(props) {
  const docs = [
    {
      URI: process.env.PUBLIC_URL + '/sample.pdf'
    }
    // Add more items to the array as needed
  ];

  // Rest of your component code
}
